language: java

cache:
  directories:
  - $HOME/.m2

jdk:
  - oraclejdk8
  - oraclejdk10
  - openjdk8
  - openjdk10

before_install:
  - java -version
  - mvn --version

install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
